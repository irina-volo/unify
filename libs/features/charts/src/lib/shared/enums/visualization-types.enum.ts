//this enum outlines the possible visualisation types across all json files
export enum VisualizationTypes {
    None = 0,
    DualAxisHorizontalBar,
    DualAxisVerticalbar,
    DualAxisLine,
    Grid,
    HorizontalBar,
    VerticalBar,
}

// this enum heps to classify the JSON files.
export enum VisualizerConfigTypes {
    None = 0,
    Bar,
    Column,
    DualAxisBar,
    DualAxisColumn,
    DualAxisLine,
    StackedBar,
    StackedColumn,
}


